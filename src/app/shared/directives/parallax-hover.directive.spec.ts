import { ParallaxHoverDirective } from './parallax-hover.directive';

describe('ParallaxHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxHoverDirective();
    expect(directive).toBeTruthy();
  });
});
